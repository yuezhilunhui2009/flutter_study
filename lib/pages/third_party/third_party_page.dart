import 'package:flutter/material.dart';

class ThirdPartyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('third_party_view'),
    );
  }
}
