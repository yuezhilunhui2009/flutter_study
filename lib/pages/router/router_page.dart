import 'package:flutter/material.dart';

class RouterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('router_view'),
    );
  }
}
