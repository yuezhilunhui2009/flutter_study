import 'package:flutter/material.dart';

class StorageView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('storage_view'),
    );
  }
}
