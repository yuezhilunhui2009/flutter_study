import 'package:flutter/material.dart';

class ListViewView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ListView Widget'),
      ),
      body: Container(
        child: Text('list_view_view'),
      ),
    );
  }
}
