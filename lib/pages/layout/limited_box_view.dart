import 'package:flutter/material.dart';

class LimitedBoxView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LimitedBox Widget'),
      ),
      body: Container(
        child: Text('limited_box_view'),
      ),
    );
  }
}
