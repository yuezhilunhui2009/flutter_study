import 'package:flutter/material.dart';

class ListBodyView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ListBody Widget'),
      ),
      body: Container(
        child: Text('list_body_view'),
      ),
    );
  }
}
