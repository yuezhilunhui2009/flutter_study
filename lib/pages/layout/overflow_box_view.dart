import 'package:flutter/material.dart';

class OverflowBoxView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('OverflowBox Widget'),
      ),
      body: Container(
        child: Text('overflow_box_view'),
      ),
    );
  }
}
