import 'package:flutter/material.dart';

class TransformView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transform Widget'),
      ),
      body: Container(
        child: Text('transform_view'),
      ),
    );
  }
}
