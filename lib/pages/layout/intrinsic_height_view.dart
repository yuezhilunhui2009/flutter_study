import 'package:flutter/material.dart';

class IntrinsicHeightView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('IntrinsicHeight Widget'),
      ),
      body: Container(
        child: Text('intrinsic_height_view'),
      ),
    );
  }
}
