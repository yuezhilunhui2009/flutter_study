import 'package:flutter/material.dart';

class IndexedStackView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('IndexedStack Widget'),
      ),
      body: Container(
        child: Text('indexed_stack_view'),
      ),
    );
  }
}
