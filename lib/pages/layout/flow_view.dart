import 'package:flutter/material.dart';

class FlowView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flow Widget'),
      ),
      body: Container(
        child: Text('flow_view'),
      ),
    );
  }
}
