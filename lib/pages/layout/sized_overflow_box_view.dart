import 'package:flutter/material.dart';

class SizedOverflowBoxView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SizedOverflowBox Widget'),
      ),
      body: Container(
        child: Text('sized_overflow_box_view'),
      ),
    );
  }
}
